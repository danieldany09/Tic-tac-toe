# Browser connects directly to Nakama (same host as this page, or your server IP).
# For local dev with `npm run dev`, defaults match docker compose (127.0.0.1:7350).

VITE_NAKAMA_HOST=127.0.0.1
VITE_NAKAMA_PORT=7350
VITE_NAKAMA_KEY=defaultkey
# VITE_NAKAMA_USE_SSL=true
